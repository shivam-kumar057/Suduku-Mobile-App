Place branded app icon and splash assets here before release.

Suggested assets:
- `icon-1024.png` for Play Store listing
- `adaptive-foreground.png` and `adaptive-background.png` for Android adaptive icons
- `splash-logo.png` for launch screen composition

The native Android and iOS folders are ready to accept generated assets, but production artwork is intentionally kept separate from source so it can be swapped without touching app logic.
